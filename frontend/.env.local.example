# Backend API URL (server-side only, not exposed to browser)

# 로컬 개발 환경
BACKEND_API_URL=http://localhost:8000/api/v1

# Docker 환경에서는 docker-compose.yml에서 자동으로 설정됨:
# BACKEND_API_URL=http://host.docker.internal:8000/api/v1
